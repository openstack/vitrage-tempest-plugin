metadata:
 id: free alarms for hosts
definitions:
 entities:
  - entity:
     category: RESOURCE
     type: nova.host
     template_id: host
scenarios:
 - scenario:
    condition: host 
    actions:
     - action:
        action_type: raise_alarm
        action_target:
         target: host
        properties:
         alarm_name: host_alarm
         severity: so_so

