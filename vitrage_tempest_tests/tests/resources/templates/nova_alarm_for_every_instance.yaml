metadata:
 id: free alarms for instances
definitions:
 entities:
  - entity:
     category: RESOURCE
     type: nova.instance
     template_id: vm
scenarios:
 - scenario:
    condition: vm 
    actions:
     - action:
        action_type: raise_alarm
        action_target:
         target: vm
        properties:
         alarm_name: vm_alarm
         severity: so_so

